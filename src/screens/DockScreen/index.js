import React, { Component } from 'react';
// import PropTypes from 'prop-types'
import { View, Text } from 'react-native';

export default class index extends Component {
  static propTypes = {
    // prop: PropTypes
  };

  state = {};

  render() {
    return (
      <View>
        <Text>Dock Screen</Text>
      </View>
    );
  }
}
